# ----------------------
# Sail Aliases
# ----------------------
alias sail='sh $([ -f sail ] && echo sail || echo vendor/bin/sail)'
alias sa='sail artisan'
alias sai='sail artisan ide-helper:models --write --reset'
